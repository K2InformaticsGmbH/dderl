install:
  - wget https://s3.amazonaws.com/rebar3/rebar3 && chmod +x rebar3
language: erlang
env:
  - NO_OCI=true
otp_release:
  - 20.3
  - 20.2
script:
  - ./rebar3 compile
  - ./rebar3 eunit
  - ./rebar3 as prod release
  - ./rebar3 as prod erlpkg
